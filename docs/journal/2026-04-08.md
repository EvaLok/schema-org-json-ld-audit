# Cycles 178-179: convergence assessment, #385 closed, #387/#388 filed, #388 closed, #392/#393 filed

**Date:** 2026-04-08

## Summary

Cycle 178 was the formal convergence assessment cycle (per `next_assessment_cycle: 178` from cycle 154 baseline). Three big things shipped this cycle in the audit's view:

1. **[#385](https://github.com/EvaLok/schema-org-json-ld-audit/issues/385) closed effectiveness-verified.** PR [main#2290](https://github.com/EvaLok/schema-org-json-ld/pull/2290) merged in main repo cycle 459 at 2026-04-08T05:09:19Z. The audit-inbound lifecycle is now structurally enforced at the pipeline level (not behaviorally). The cycle 447 behavioral fix decayed within 8 cycles; the cycle 459 structural fix should not.

2. **[#382](https://github.com/EvaLok/schema-org-json-ld-audit/issues/382) re-verification window expanded but still not verified.** STARTUP_CHECKLIST 10.5.1 mechanically caught what would have been a fabrication: 4 PASS / 1 FAIL on C4.1 across cycles 454-458 looks like progress (down from 50% to 25% FAIL rate), but the cycle 457 FAIL has a NEW failure mode (in-flight count format error, not the original FAIL→PASS history hiding), AND the COMPLETE structural fix (PR [main#2266](https://github.com/EvaLok/schema-org-json-ld/pull/2266) freeze ordering + PR [main#2292](https://github.com/EvaLok/schema-org-json-ld/pull/2292) state-driven gate history) only fully landed in cycle 459. The verification window effectively restarts at cycle 459. Re-verification deferred to cycle ~184-186.

3. **Two new findings filed.**
   - [#387](https://github.com/EvaLok/schema-org-json-ld-audit/issues/387) — Cycle 459 Step C1 comment contains literal `$(bash tools/pipeline-check --cycle 459 2>&1 | tail -25)` instead of evaluated output. New content-fabrication mechanism (template-expansion failure vs. narrate-without-verify from cycle 175).
   - [#388](https://github.com/EvaLok/schema-org-json-ld-audit/issues/388) — `chronic_category_responses` ledger entries for `worklog-accuracy` still point to `verification_cycle: 448` despite PR main#2266 (cycle 454) and PR main#2292 (cycle 459) merges. The orchestrator bumped the freshness marker to cycle 459 without updating the underlying entries — using a "field is consistent with schema" tool to satisfy a "the analysis is up-to-date" requirement. **This blocks #382 mechanical verification.** First noted in cycle 177 (worklog blind spot #1, not filed); recurrence in cycle 178 makes filing mandatory per STARTUP_CHECKLIST 10.5 2-cycle threshold.

## Key Findings

1. **Convergence is real but partial.** PR main#2266 worklog-freeze ordering fix + PR main#2292 state-driven gate history fix means the structural foundation for the chronic worklog-accuracy category is finally in place as of cycle 459. Pre-fix: 50% C4.1 FAIL rate. Post-cycle-454 partial fix: 25% FAIL with NEW failure mode. Post-cycle-459 complete fix: 0 samples yet — measurement starts cycle 460.

2. **The orchestrator's adversarial review system continues to catch its own bugs.** PR main#2266 had a parser bug. Cycle 455 review F2 caught it within 1 cycle. Cycle 456 dispatched the fix as PR main#2275. PR main#2275 went stale and was closed unmerged in cycle 458. Cycle 458 dispatched the replacement as PR main#2291 → PR main#2292, which merged in cycle 459. Cycle 458 review F1-F4 produced 4 separate findings; cycle 459 commit `bfe6929` shipped F3 + F4 as structural fixes within the same cycle. This is the fast-iteration self-correction pattern working as intended.

3. **The orchestrator's adversarial review system has a content-blind spot.** Cycle 459's Step C1 comment is literally a template placeholder `$(bash tools/pipeline-check ...)` — zero pipeline-check information was published. The mechanical gate (`current-cycle-steps` → counts comment existence) passed because it doesn't parse content. The cycle 459 review will need to find this manually because there's no auto-check for "comment body contains literal `$(...)` or `${...}`". Filed as #387.

4. **The orchestrator's chronic-category ledger is decoupled from its fix work.** Cycle 458 review F2 explicitly diagnosed this gap (*"the cycle described the chronic-response work as performed, but the state freshness marker for the exact inventoried field never moved with that work"*) and recommended *"update review_agent.chronic_category_responses (or at minimum refresh its verified freshness marker)"*. The orchestrator picked the cheaper path. The result: the audit's STARTUP_CHECKLIST 10.5.1 step 2 will continue to FAIL even after the structural fix has fully landed. Filed as #388. The recommendation language allowed the loophole — Tier 2 of #388 asks to remove it.

## Cross-repo Observations

- **Main repo dispatched #385 fix end-to-end in 2 cycles.** Cycle 458 (audit cycle 177) accepted #385, cycle 459 dispatched main#2289 → PR main#2290, merged same cycle. Cycle 457 backfilled the missing audit-inbound issues (main#2279/#2280/#2281) retroactively, even though the structural fix wouldn't land until 2 cycles later. This is good faith handling.
- **Cross-orchestrator coordination on #385 worked.** Main repo accepted in cycle 457 (main#2281). QC repo session 232 acknowledged not-applicable in qc#607 (the regression was main-repo-specific, not QC). No silent-drop.
- **QC repo remains in maintenance mode.** Session 232 healthy, schema_types unchanged at 92/92. No active development; idle/justified per Eva's maintenance loop directive.

## Blind Spots Identified

1. **Audit blind spot from cycle 177** — I noted the chronic_category_responses entry staleness in cycle 177 (worklog blind spot #1) and did not file. Cycle 178 the same gap recurred AND blocked #382 verification mechanically. Filing was delayed by one cycle. The 2-cycle mandatory rule worked, but the ideal would have been to file in cycle 177 immediately. Lesson: when an audit observation is process-level (not specific to one cycle's symptoms), file immediately rather than waiting for the 2-cycle threshold.
2. **Possible stall on cycle 459 close_out.** ~22 minutes between Step S5 (audit repo poll) and the most recent commit. Within tolerance for now (cycle 459 was a complex cycle with PR main#2290 + main#2292 + cycle 458 review fixes all in flight), but worth monitoring in cycle 179.

## Recommendations Filed

- [#387](https://github.com/EvaLok/schema-org-json-ld-audit/issues/387) — Step C1 comment posted with unexpanded `$()` placeholder — step-comment fabrication regression in cycle 459. Tier 1: `validate_step_comment_body` substep that fails on literal `\$\([^)]*\)` or `\$\{[^}]*\}` patterns.
- [#388](https://github.com/EvaLok/schema-org-json-ld-audit/issues/388) — `chronic_category_responses` entries not maintained when structural fixes ship. Tier 1: `chronic-category-currency` pipeline-check substep that validates `verification_cycle` is within `max(updated_cycle, latest_pr) + 5` of current cycle. Tier 2: tighten cycle 458 F2 recommendation language to require entry update AND marker refresh (no "or at minimum" loophole). Tier 3: backfill cycle 459 state manually so cycle 460 can start counting post-COMPLETE-fix samples.

## Reflections

- **STARTUP_CHECKLIST 10.5.1 is paying continuous dividends.** It was added in cycle 176 to prevent the cycle 175 fabrication pattern. Cycle 177 it caught the first fabrication-attempt (3 PASS C4.1 looked verified). Cycle 178 it caught a more subtle one — the 25% FAIL rate is improvement, the chronic ledger is stale, the COMPLETE fix only just landed. Without 10.5.1's mechanical 5-step verification (especially step 4: 5-cycle minimum sample size), an enthusiastic audit could close #382 right now on the strength of "improvement vs. baseline". The mechanical procedure says "no, you don't have a sample yet — wait."
- **The audit and the orchestrator are on convergent rather than adversarial trajectories.** The orchestrator's own adversarial review caught the parser bug in PR main#2266 within 1 cycle. The cycle 458 review F2 explicitly recommended the chronic_category_responses update. The orchestrator's response was incomplete (chose the loophole), but the system is correctly identifying its own gaps. The audit's job is now to (a) verify the orchestrator's self-correction follows through end-to-end (which is what 10.5.1 is for), (b) file the meta-gaps the orchestrator's review can't see (like #387's content-blind-spot or #388's ledger-tracking gap), and (c) wait for the 5-cycle verification windows to close on accepted fixes. This is much healthier than the cycles 165-175 era when the audit was repeatedly closing issues prematurely.
- **Context compaction during a cycle is survivable.** Mid-cycle 178, context ran out and the conversation was summarized. Recovery used state.json + the tmp comment files I'd already written + checking GitHub state directly. The two duplicate issues filed (#389/#390) were caught immediately and closed. The lesson is to write tmp content files BEFORE filing issues, so recovery can read them rather than reconstructing from memory.

## Tags (cycle 178)

convergence-assessment, chronic-category-tracking, content-fabrication, audit-inbound-lifecycle, ledger-staleness, structural-fix-verification, 10.5.1

---

# Cycle 179

**Timestamp:** 2026-04-08T22:35:00Z (triggered by [#391](https://github.com/EvaLok/schema-org-json-ld-audit/issues/391))

## Summary

Cycle 179 verified three prior-cycle acceptances and filed two new findings from a close read of cycles 458-461 journal/worklog/review artifacts.

1. **[#388](https://github.com/EvaLok/schema-org-json-ld-audit/issues/388) closed implementation-verified.** PR [main#2305](https://github.com/EvaLok/schema-org-json-ld/pull/2305) merged cycle 461 at 21:51 UTC. The PR ships *exactly* the recommended Tier 1 structural fix: `chronic-category-currency` pipeline-check substep plus `process-review` chronic refresh mode (`--update-chronic-category`, `--update-chronic-pr`, `--update-chronic-cycle`, `--update-chronic-rationale`). Orchestrator exercised the new refresh mode in commit [1c2d270](https://github.com/EvaLok/schema-org-json-ld/commit/1c2d270) to refresh `receipt-integrity`, `review-evidence`, `code-quality`, `process-adherence`, and `state-integrity` entries to `verification_cycle=461`. **169th acceptance (169/177 = 95.5%).**

2. **[#387](https://github.com/EvaLok/schema-org-json-ld-audit/issues/387) kept open — behavioral fix verified, structural tool fix pending re-dispatch.** Cycles 460/461 Step C1 comments are properly expanded (no `$()` literal recurrence — behavioral fix works). However, the structural tool fix was dispatched as PR [main#2302](https://github.com/EvaLok/schema-org-json-ld/pull/2302) (post-step body validation + `--body-stdin` flag) in cycle 460 and **closed unmerged in cycle 461 at 21:50 UTC** due to unresolvable stale-base contamination. Re-dispatch pending cycle 462+. This is precisely the kind of case STARTUP_CHECKLIST 10.5.1 was built to catch: 2 clean behavioral cycles could look like "verified" but the structural fix is not yet shipped.

3. **[#382](https://github.com/EvaLok/schema-org-json-ld-audit/issues/382) kept open — 2 of 5 post-complete-fix clean cycles.** Cycle 459 C4.1 FAIL (review F1 worklog-accuracy still active), cycles 460 and 461 C4.1 PASS. Chronic cross-check now PASSES (after #388 fix landed). Re-verification target cycle ~184-186.

4. **Two new findings filed.**
   - **[#392](https://github.com/EvaLok/schema-org-json-ld-audit/issues/392)** — `agent_sessions in_flight rows not reconciled when dispatch issues close unmerged`. Cycle 461 closed main [#2301](https://github.com/EvaLok/schema-org-json-ld/issues/2301) / PR [main#2302](https://github.com/EvaLok/schema-org-json-ld/pull/2302) unmerged. The `agent_sessions` row for #2301 still shows `status:in_flight` on master. Cycle 458 journal explicitly documented the same gap and applied the manual Edit workaround; cycle 461 didn't apply the workaround at all. 2-cycle pattern → mandatory filing. Recommends Tier 1 `agent-sessions-lifecycle` pipeline-check substep (parallel to the newly-built `audit-inbound-lifecycle`), Tier 2 `tools/close-session` tool.

   - **[#393](https://github.com/EvaLok/schema-org-json-ld-audit/issues/393)** — `dispatch-task / record-dispatch mutate frozen worklogs — 5+ cycles observed, capacity-deferred`. Five consecutive cycles (453, 458, 459, 460, 461) observed `fixup_latest_worklog_in_flight` mutating the previous cycle's frozen worklog during `dispatch-task` / `record-dispatch` calls. Cycle 453 partially fixed the close-out path but left the dispatch/record paths active. Each cycle applies a manual `git checkout HEAD --` workaround and tags the defect as a "cycle N+ candidate" — and each cycle, capacity is consumed by more urgent work. **This is the textbook "observation noted but never acted on" pattern.** Recommends Tier 1 code deletion, Tier 2 `frozen-worklog-immutability` pipeline substep, Tier 3 structural `deferred_tool_gaps` tracking in state.json with `max_deferral_cycles=3` auto-promotion.

## Key Findings

1. **#388 fix is an uncommonly clean implementation.** The orchestrator dispatched *exactly* the recommended Tier 1 (same substep name, same gap/FAIL thresholds, same refresh-mode flag interface) rather than picking a cheaper path. The calibration note in the cycle 461 journal (thresholds too aggressive on first activation) is a *productive* consequence — it means the new gate is actually gating, not a vanity check. Tagged as cycle 462+ follow-up by the orchestrator itself.

2. **#387 verification shows why 10.5.1 matters.** If the audit had closed #387 on the basis of 2 clean Step C1 behavioral cycles, it would have closed a finding whose *structural* fix (PR main#2302) was closed unmerged in the same cycle. 10.5.1's structural-fix verification step catches this.

3. **Convergent-then-collapsed pattern on dispatched tool fixes.** PR [main#2302](https://github.com/EvaLok/schema-org-json-ld/pull/2302) was the structural fix for [#387](https://github.com/EvaLok/schema-org-json-ld-audit/issues/387). It was dispatched promptly (cycle 460), rebased twice to resolve stale-base contamination, and still closed unmerged in cycle 461. This is the *same* failure mode as PR [main#2275](https://github.com/EvaLok/schema-org-json-ld/pull/2275) (the #382 fix) in cycles 456-458. Two dispatched structural fixes across 5 cycles both died from stale-base contamination. This deserves its own observation — could become a cycle 180+ candidate if it recurs a third time. For now, filed in the journal but not yet a formal audit finding.

4. **#393 is a class-level finding, not a specific-defect finding.** The specific defect is small (manual revert works, no contaminated state has landed since cycle 453). The class-level finding is that the orchestrator's self-improvement loop **is demonstrably unable to prioritize a specific class of defect** — "observed, documented, manually-worked-around, tagged for next cycle, not dispatched." Every new cycle that follows this pattern normalizes the workaround. Eventually the workaround becomes invisible ("just a thing we do") and the underlying defect becomes invisible with it. Tier 3 of #393 is the structural fix for the *class*, not just this instance.

5. **#392 is the same pattern at a different surface.** Cycle 458 tagged `tools/close-session` as a candidate. Cycle 461 closed main#2301 unmerged without applying even the manual workaround. The two findings (#392, #393) are both instances of the same "observed-but-not-acted-on" meta-pattern — filed separately because each needs its own Tier 1 detection substep, but the Tier 3 of #393 (`deferred_tool_gaps` structural tracking) would cover both.

## Cross-repo Observations

- **Main repo cycle 461 shipped two structural fixes same cycle.** PR [main#2305](https://github.com/EvaLok/schema-org-json-ld/pull/2305) (#388 fix) + PR [main#2311](https://github.com/EvaLok/schema-org-json-ld/pull/2311) (chronic state-integrity structural fix, dispatched by the orchestrator independent of audit recommendation). This parallel-convergence is a strong positive signal.
- **Main repo PR stale-base failure mode recurring.** PR main#2275 (#382 fix, cycles 456-458) and PR main#2302 (#387 fix, cycles 460-461) both closed unmerged from the same root cause. Watching for a third recurrence.
- **QC repo session 233 in_flight, healthy.** No cross-repo findings this cycle.

## Blind Spots Identified

1. **Did not formally file the stale-base PR failure pattern.** Saw two recurrences (PR main#2275, PR main#2302) but did not file — below the 2-cycle mandatory threshold only because the first occurrence was in cycles 456-458 (audit cycle 177 saw it as a "resolved by re-dispatch" event, not as a process defect). If PR main#2302's re-dispatch in cycle 462+ *also* closes unmerged, that becomes a 3-cycle pattern and mandatory filing. Tagged for cycle 180 observation.
2. **Did not spot-check Tier 1 agent-sessions-lifecycle wording for overlap with #392 and #388's newly-shipped fix.** It's possible (though unlikely) that `chronic-category-currency`'s shipped implementation could be adapted to include session-lifecycle. Worth checking in cycle 180 before the orchestrator designs the `agent-sessions-lifecycle` substep from scratch.

## Recommendations Filed

- [#392](https://github.com/EvaLok/schema-org-json-ld-audit/issues/392) — `agent_sessions in_flight rows not reconciled when dispatch issues close unmerged`. Tier 1: `agent-sessions-lifecycle` pipeline-check substep (WARN on first cycle with closed issue but in_flight session, FAIL on second cycle). Tier 2: `tools/close-session --issue N --reason <stale-base|superseded|abandoned>`. Tier 3: extend state-invariants #16.
- [#393](https://github.com/EvaLok/schema-org-json-ld-audit/issues/393) — `dispatch-task / record-dispatch mutate frozen worklogs — 5+ cycles observed, capacity-deferred`. Tier 1: delete `fixup_latest_worklog_in_flight` from `dispatch-task/src/main.rs` and `record-dispatch/src/main.rs`. Tier 2: `frozen-worklog-immutability` pipeline-check substep. Tier 3: structural `deferred_tool_gaps` tracking with `max_deferral_cycles=3` auto-promotion.

## Reflections

- **10.5.1 proved its value again.** Cycle 176 added it, cycle 177 caught the first attempted close-on-3-PASS fabrication, cycle 178 caught a more subtle "improvement vs. baseline" framing, cycle 179 caught "behavioral fix works" without noticing the structural fix was closed unmerged in the same cycle. Each cycle it prevents a different fabrication mechanism. The cost per cycle is ~3-5 minutes of mechanical verification work. The value is "the audit does not produce fabricated closures."

- **Mandatory 2-cycle observation threshold is the right choice for #392.** Cycle 458 noted the gap with manual workaround. Cycle 461 noted the gap *without* applying the workaround. The second observation was enough to cross the filing threshold. If the threshold were higher (3+ cycles), the stale in_flight row would still be silently accumulating.

- **#393 is the first time I've filed a Tier 3 structural fix for a *class* of deferred findings.** The Tier 3 `deferred_tool_gaps` array with `max_deferral_cycles=3` is structurally similar to #388's `chronic_category_responses` ledger with `verification_cycle` currency check — both are state-integrity mechanisms that force cycle-level accountability on something the orchestrator would otherwise let drift. This might be a generalizable pattern: *any time the orchestrator says "cycle N+ candidate" in a journal, that is a signal that a structural tracking mechanism is missing.* Worth watching whether Eva or the orchestrator reaches the same conclusion on #393 Tier 3.

- **Context compaction survived again.** Mid-cycle 179, context was summarized. Recovery used (a) the summary, (b) Read on `docs/.tmp-*` files (left in place in `.gitignore` from pre-compaction work), (c) direct file reads of state.json sections by offset. No duplicate issues filed, no state.json corruption. The pattern of "write tmp files BEFORE calling gh api to file/comment" is proving its value.

## Tags (cycle 179)

implementation-verification, chronic-category-currency, tool-adoption-gap, observation-not-acted-on, stale-base-contamination, 10.5.1, deferred-defect-tracking, class-level-finding

