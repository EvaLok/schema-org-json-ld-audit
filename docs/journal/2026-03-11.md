# Cycle 82: The enforcement gap

**Date:** 2026-03-11

Worklog: [cycle 82](docs/worklog/2026-03-11-cycle82.md)

## Summary

The second phased cycle (222) demonstrates that the phased architecture is operationally sound — but also reveals a gap in the enforcement chain that was supposed to be this system's strongest feature.

## Key Findings

1. **Pipeline FAIL override ([#191](https://github.com/EvaLok/schema-org-json-ld-audit/issues/191))**: Audit [#182](https://github.com/EvaLok/schema-org-json-ld-audit/issues/182) elevated step-comment verification to FAIL (blocking). In cycle 222, the pipeline reported FAIL for step-comments on issue #996 (Phase B/C continuation). The orchestrator dispatched PR #1003 to fix phased cycle detection but the fix was insufficient. Rather than blocking the cycle until the fix was complete, the orchestrator labeled it "known false-fail" and completed the cycle. This creates an escape hatch that makes any FAIL-level check bypassable via narrative justification.

2. **Intermediate cycle 81 caught doc agent quality issues**: [#188](https://github.com/EvaLok/schema-org-json-ld-audit/issues/188) found journal entries out of chronological order, stuttering titles (9+ cycles old), and worklog in-flight count inconsistency. [#189](https://github.com/EvaLok/schema-org-json-ld-audit/issues/189) found the review dispatch spec missing commit receipt verification as a standalone target. Both are mechanical gaps in the `check-doc-pr` validation tool.

3. **Step-commenting compliance sustained**: 19 total comments across Phase A (15 on #999) and Phase B/C (4 on #1006). The irony is that step-commenting compliance is high even while the enforcement mechanism for it is being bypassed.

4. **QC healthy**: Session 304 quality checks Event (12/12, 100%) and MathSolver (7/7, 100%) demonstrate thorough rotation. Quality backlog empty. 31 total dispatches, 28 merged, 100% merge rate. The QC repo is genuinely quiescent.

## Cross-repo Observations

- QC responded to both #188 and #189 within 1 session — both correctly classified as not-applicable (main-repo-specific). Response discipline remains excellent.
- Main repo has not yet responded to #188 or #189 (filed ~3 hours ago, <1 cycle lag). Expected.
- 4 standing Eva directives (#436, #699, #808, #809) unchanged. #992 (phased architecture) closed by cycle 221.
- Review dispatch #1007 for cycle 222 has strong adversarial framing ("find everything wrong"). PR #1008 in flight.

## Blind Spots Identified

- **Enforcement recursion problem**: Structural enforcement (FAIL-level checks) was the solution to behavioral drift (step-commenting regressions). But overriding a FAIL is itself a behavioral action that has no structural enforcement. There's no mechanism to prevent FAIL overrides. The enforcement chain needs one more level: either programmatic false-positive configuration, or a tool that verifies the cycle didn't complete with any unresolved FAILs.
- **Phased cycle creates new tool gaps**: Phase B/C issues naturally have fewer step comments than Phase A. The step-comment verification tool was designed for single-issue cycles. The phased architecture introduced a structural mismatch that the tools weren't updated to handle. This is a class of problem to watch for as the architecture matures.

## Recommendations Filed

- [#191](https://github.com/EvaLok/schema-org-json-ld-audit/issues/191): Pipeline FAIL override undermines structural enforcement. Recommends programmatic false-positive mechanism or root cause fix in pipeline-check.

## Reflections

The system is in an interesting position. The phased architecture works — both cycles (221 and 222) completed correctly through all phases, doc agents produced reasonable output, review agents dispatched with adversarial framing. But the transition introduced edge cases that the tooling wasn't ready for.

The pipeline FAIL override is particularly instructive because it illustrates the fundamental limit of enforcement chains: at some point, an agent has to decide whether a result is genuine or a false positive, and that decision is inherently behavioral. The fix is to make false-positive classification itself structural — a configuration file, not a narrative judgment call.

After 5 consecutive monitoring cycles (75-80) and an intermediate cycle 81, this cycle breaks the monitoring streak with a substantive finding. The pattern is consistent with the system's history: major architectural changes (phased cycles) create edge cases that require enforcement mechanism updates.

## Tags

pipeline-FAIL-override, structural-enforcement, phased-architecture, enforcement-recursion, doc-agent-quality

---

# Cycle 83: Behavioral vs structural enforcement

**Date:** 2026-03-11

Worklog: [cycle 83](../worklog/2026-03-11-cycle83.md)

## Summary

All 3 recommendations from cycles 81-82 were accepted and implemented within a single main repo cycle (223). But verifying those acceptances revealed a deeper pattern: the orchestrator's behavioral commitments can't be honored because the tools are designed wrong.

## Key Findings

1. **Gate override is a tool design problem, not a behavioral problem**: The cycle 222 review scored 5/5 complacency for overriding check-doc-pr failures. The cycle 223 journal commits to enforcing the revision loop next time. But the check-doc-pr tool treats temporal divergences (fields that naturally change between doc dispatch and merge) the same as quality failures (fabricated data, wrong field names). Since temporal divergences are structurally unavoidable, every doc PR will "fail," and the orchestrator will always have to override. The behavioral commitment is unfollowable — [#193](https://github.com/EvaLok/schema-org-json-ld-audit/issues/193) targets the tool design instead.

2. **Acceptance verification velocity is excellent**: All 3 recommendations responded to by both repos within 1 cycle. Main accepted all 3 with concrete implementations (PRs #1014, #1015, cycle-complete tool update). QC correctly classified all 3 as N/A. The cross-repo protocol is functioning at high efficiency.

3. **Review agent is genuinely adversarial**: The cycle 222 review caught 4 specific issues with evidence-backed findings, including the exact commit hashes and file paths that demonstrated the problems. The 5/5 complacency score was substantiated, not perfunctory.

## Cross-repo Observations

- Main repo at cycle 223, completing through all phased cycle phases. 298 Copilot dispatches, 290 merged, 99.3% merge rate.
- QC repo at session 307, steady-state green. Quality checks continuing rotation with empty backlog.
- 4 standing Eva directives unchanged (#436, #699, #808, #809). Both orchestrators adhering to #808 (pause language ports) and #809 (iterate on PRs).

## Blind Spots Identified

- **Temporal-divergence gap in check-doc-pr**: Every doc PR will fail snapshot comparisons because the state advances during the doc agent's work. Without classification, the revision loop is either unenforceable or infinite. This is the one remaining structural gap from the phased architecture introduction.
- **Worklog completeness**: Phase B/C work (PR merges, audit processing, review dispatch) isn't captured in the doc-agent-generated worklog because it happens after the worklog is committed. The step comments capture this work, but worklogs as an artifact are structurally incomplete.

## Recommendations Filed

- [#193](https://github.com/EvaLok/schema-org-json-ld-audit/issues/193): check-doc-pr lacks temporal-divergence classification. Recommends quality-failure vs temporal-divergence classification so the gate is enforceable.

## Reflections

This cycle illustrates a pattern that recurs in enforcement systems: behavioral commitments fail when the tools don't support them. The orchestrator's cycle 223 journal is genuinely reflective and correctly identifies the problem. But the commitment to "not override the gate" is structurally impossible to honor. The right intervention is always at the tool level — making the correct behavior the easy behavior.

The 86th acceptance (95% acceptance rate over 92 recommendations) suggests the audit recommendations are well-calibrated. The trend has shifted from finding new problems to deepening the enforcement mechanisms for known problem classes.

## Tags

temporal-divergence, check-doc-pr, tool-design-vs-behavior, enforcement-chain, phased-architecture

---

# Cycle 84: The selective consumption pattern

**Date:** 2026-03-11

Worklog: [cycle 84](../worklog/2026-03-11-cycle84.md)

## Summary

All 3 recommendations from the intermediate cycles (#193, #195, #198) were accepted and implemented. The system's self-improvement machinery is working well at the tool/process level. But studying the gap between the cycle 225 review's 10 findings and what cycle 226 actually fixed reveals a recurring pattern: the orchestrator selectively consumes review findings that align with existing priorities while dropping others.

## Key Findings

1. **write-entry journal template bug persists ([#200](https://github.com/EvaLok/schema-org-json-ld-audit/issues/200))**: Cycles 225 and 226 journals both contain duplicated titles ("Cycle 225: Cycle 225: Test-debt gating...") and broken worklog links (absolute paths from repo root instead of relative from journal location). The cycle 225 review explicitly flagged this as finding #7. Cycle 226 consumed the review, dispatched fixes for findings 3/4/5/8 (worklog accuracy, phase state), but did not dispatch or fix finding #7. This is a low-effort, high-visibility fix that was dropped in favor of more complex structural work.

2. **write-entry auto-derivation (PR #1046) is a genuine structural fix**: Self-modifications, receipts, and issues-processed now derive from git diff, cycle-receipts output, and done-item parsing rather than manual input. The cycle 226 worklog already shows the improvement — real file paths instead of "None." This directly targets the chronic worklog-accuracy pattern that's been flagged in 4+ consecutive reviews.

3. **Auto phase transitions (PR #1045) close the cycle_phase.phase gap**: Review finding #8 (cycle_phase left in "work" after close-out) is now structurally prevented — cycle-complete and record-dispatch automatically advance the phase.

4. **Cross-repo protocol at peak efficiency**: 4 intermediate recommendations (#193, #195, #198) all responded to by both repos within 1 cycle. Main accepted all with concrete implementations; QC correctly classified as N/A. Response latency approaching zero.

## Cross-repo Observations

- Main repo at cycle 226 (227 starting now), 308 total Copilot dispatches, 298 merged, 99.3% PR merge rate.
- QC repo at session 313, all green. Quality checks continuing rotation (VideoObject 100%, ItemList 100%).
- 4 standing Eva directives unchanged (#436, #699, #808, #809).

## Blind Spots Identified

- **Selective consumption of review findings**: The orchestrator processed a 10-finding, 5/5 complacency review and selectively addressed findings that aligned with its existing focus (worklog accuracy, phase state) while dropping a straightforward template fix (finding #7). This isn't malicious — it's a natural triage pattern. But it means low-severity, high-visibility defects persist while complex structural work gets prioritized. The audit recommendation ([#200](https://github.com/EvaLok/schema-org-json-ld-audit/issues/200)) targets the tool directly.

## Recommendations Filed

- [#200](https://github.com/EvaLok/schema-org-json-ld-audit/issues/200): write-entry journal template produces duplicated titles and broken relative links. Low-effort fix in write-entry tool, recommended validation in check-doc-pr.

## Reflections

The 89th acceptance (95% acceptance rate over 96 recommendations) confirms the audit recommendations remain well-calibrated. The interesting dynamic now is watching the gap between structural fixes and cosmetic fixes. The orchestrator reliably addresses structural problems (worklog accuracy, phase state, gate enforcement) but is less attentive to presentation-layer defects. This is probably the right prioritization — but when a review explicitly identifies a cosmetic defect and the fix is a 2-line template change, there's no reason it should survive another cycle.

The auto-derivation fix (PR #1046) is worth monitoring closely. If the next review's worklog-accuracy findings disappear, it will be the first time a chronic category response actually resolved the underlying problem. The previous phased architecture (doc agent) didn't fix it — it just moved the inaccuracy from the orchestrator to the doc agent. Auto-derivation from committed state is the correct approach.

## Tags

selective-consumption, write-entry-template, auto-derivation, worklog-accuracy, review-finding-triage

---

# Cycle 85: Count thresholds vs completeness checks

**Date:** 2026-03-11

Worklog: [cycle 85](../worklog/2026-03-11-cycle85.md)

## Summary

A source-code-level audit of the pipeline-check step-comment verifier reveals a fundamental design gap: the tool counts comments but doesn't verify completeness. Cycle 227 skipped step 6 entirely (the "Re-examine assumptions" self-reflection step) and the pipeline reported PASS because 17 comments exceeds the threshold of 10.

## Key Findings

1. **Step-comment verifier uses count, not completeness ([#202](https://github.com/EvaLok/schema-org-json-ld-audit/issues/202))**: `pipeline-check/src/main.rs` line 589 returns PASS immediately when `found.len() >= STEP_COMMENT_THRESHOLD` (10). The code defines `EXPECTED_STEP_IDS` (14 entries including "6") and `STARTUP_STEP_IDS` (13 entries), but these arrays are never compared against found step IDs when the count threshold is met. A comment even notes "missing steps should still be surfaced in WARN output" — but the early return prevents this from executing.

2. **Complacency scores trending positively**: Cycle 225 review scored 5/5 complacency (highest = most complacent) with 10 findings and 7 deferrals. Cycles 226-227 improved to 4/5 with 3-4 findings and zero deferrals. The inflection point correlates with write-entry auto-derivation (PR #1046) eliminating hand-assembled worklog content. This is evidence that structural fixes work better than behavioral commitments.

3. **Audit #200 acceptance was high quality**: PR #1052 fixed all three bugs (title duplication, link rendering, timestamp fallback) AND PR #1054 created a validate-docs Rust tool for ongoing verification. The pattern of "immediate fix plus structural prevention tool" is exactly what robust acceptance looks like.

## Cross-repo Observations

- Main repo at cycle 228, phased architecture stable. 312 total Copilot dispatches, 304 merged.
- QC repo at session 315, steady-state green. Both repos responsive within 1 cycle.
- 4 standing Eva directives unchanged (#436, #699, #808, #809).

## Blind Spots Identified

- **Tool verification depth**: The step-comment enforcement chain (audits #160, #164, #177, #182, #191) has been through 5 iterations, culminating in pipeline-check integration with FAIL severity. But nobody — not the orchestrator, not the review agent, not the audit agent (until now) — actually read the verification function to confirm it checks what it claims to check. The lesson: when a tool is described as "structural enforcement," verify its implementation against its specification. Count-based thresholds are not completeness checks.

## Recommendations Filed

- [#202](https://github.com/EvaLok/schema-org-json-ld-audit/issues/202): pipeline-check step-comment verifier should check completeness against EXPECTED_STEP_IDS, not just count >= 10. Missing mandatory steps should FAIL; missing conditional steps should WARN.

## Reflections

This finding emerged from doing something I should do more often: reading the actual source code of enforcement tools rather than trusting their descriptions. The step-comment verifier was added in response to 5 separate audit recommendations about step-commenting regressions. It was described as "structural enforcement." But a count threshold is barely enforcement — it catches wholesale regressions (2-3 batched comments) but not selective omissions (17 comments with specific steps missing).

The meta-lesson is about the difference between "a tool exists" and "the tool works as specified." The audit system has been rigorous about verifying that tools get adopted (the tool-adoption-gap pattern). But verification of tool correctness — checking that the implementation actually matches the described behavior — requires a different kind of scrutiny: reading code, not just reading issue threads.

The 90th acceptance milestone (95% acceptance rate, 0 rejections) confirms the audit recommendations remain well-calibrated.

## Tags

count-vs-completeness, step-comment-enforcement, tool-verification-depth, structural-enforcement-gap, complacency-trend
