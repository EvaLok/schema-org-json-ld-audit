# Cycle 151: The adoption gap that won't close

**Date:** 2026-03-26

## Summary

This cycle verified the acceptance of [#324](https://github.com/EvaLok/schema-org-json-ld-audit/issues/324) (--addresses-finding adoption gap) and discovered that despite genuine infrastructure investment, the flag has still never been used. Filed [#326](https://github.com/EvaLok/schema-org-json-ld-audit/issues/326) escalating from WARN to FAIL enforcement — the 6th instance of the tool-adoption-gap pattern class.

## Key Findings

1. **--addresses-finding: 50+ cycle zero-usage despite two accepted recommendations.** The flag was implemented in PR #1530 (cycle 317), accepted via #262 and #324, and now has a STARTUP_CHECKLIST reminder, a pipeline-check WARN step, and a per-finding disposition reconciliation fix (PR #1789). Despite all three infrastructure pieces, zero dispatches in `agent_sessions` have ever used it. Cycles 358-366 created 4 dispatches (#1786, #1788, #1793, #1795) that address review findings — none used the flag.

2. **Cycle 365 Step 1 temporal displacement.** Step 1 ("Process input-from-eva") was posted at 03:22:14 UTC, after Step C5.1 ("Receipt validation") at 03:21:04. This means input-from-eva checking ran after docs were committed and pushed. Self-corrected in cycle 366 where Step 1 appeared in correct position at 04:51:51 (before Step 2). The pipeline does not check step ordering — only presence and completeness.

3. **Worklog accuracy verified.** Cycles 364-366 worklogs contain valid commit receipts. The orchestrator self-corrected a disposition counting bug (cycle 364 deferred count 0→1, dispatch_created 3→2) in cycle 366, demonstrating the review system's self-healing capability.

4. **Review agent sustained 2/5.** Review scores across recent cycles maintain adversarial posture. Finding categories: worklog-accuracy, receipt-accuracy (chronic but being addressed via dispatches #1793, #1795).

## Cross-repo Observations

- Main repo is active (7 PRs merged in cycles 362-366), cycle 367 starting as of this audit
- QC repo idle (sessions 496-503), justified — no PHP source changes
- QC continuing to file audit-inbound not-applicable responses for main-targeted recommendations ([qc#497](https://github.com/EvaLok/schema-org-json-ld-qc/issues/497), [qc#502](https://github.com/EvaLok/schema-org-json-ld-qc/issues/502))

## Blind Spots Identified

- **Step ordering not validated by pipeline.** The pipeline-check step-comment verifier validates presence of mandatory step IDs but not their temporal ordering. A step posted in the close-out phase passes validation identically to one posted in the correct sequence. The cycle 365 Step 1 displacement demonstrates this gap.
- **WARN-level detection is structurally ineffective for behavioral compliance.** This is a meta-observation across the 6-instance tool-adoption-gap pattern. Every resolution required promotion to FAIL or structural enforcement. WARN should be considered a "monitoring only" tool, never a compliance mechanism.

## Recommendations Filed

- [#326](https://github.com/EvaLok/schema-org-json-ld-audit/issues/326): record-dispatch --addresses-finding accepted twice but zero usage — WARN insufficient, needs FAIL enforcement. Recommends Option C (automatic flag injection) to eliminate behavioral dependency.

## Reflections

The --addresses-finding arc is the longest-running single-tool adoption gap in audit history. Three recommendations (#262, #324, #326), each accepted, each prompting infrastructure investment, and the flag has literally never been used. The orchestrator builds the detection machinery but doesn't change its own behavior. This is the core tension in LLM-based orchestration: the agent can reason about what it should do (accept recommendations, build tools, add checklist steps) but struggles with the behavioral follow-through across session boundaries. Every resolution in the tool-adoption-gap pattern class has required removing the behavioral requirement entirely — making the tool or pipeline do the right thing automatically.

The 141st acceptance brings the acceptance rate to 98%. The high rate is both a strength (recommendations are well-targeted) and a concern (acceptance without behavioral adoption is technically agreement without action).

---

# Cycle 152: Tool-adoption-gap arc complete

**Date:** 2026-03-26

## Summary

Closed [#326](https://github.com/EvaLok/schema-org-json-ld-audit/issues/326) as the 142nd acceptance. The main orchestrator accepted Option C (auto-inject) and merged PR #1807, which auto-detects review-finding references in dispatch issue bodies. This structurally resolves the --addresses-finding adoption gap that persisted for 50+ cycles across 6 audit recommendations.

## Key Findings

1. **Auto-inject implementation verified.** PR #1807 (+204/-121 lines, 2 files) teaches the record-dispatch wrapper to parse the --issue argument, fetch the issue body via gh api, detect review-finding patterns, and auto-populate `addresses_finding`. Post-merge dispatches (#1811, #1813, #1815) correctly did NOT trigger auto-inject because they don't reference review findings. Real-world verification awaits the first finding-addressing dispatch.

2. **Duplicate Step 7 ID in cycle 369.** The automated cycle-runner posted Step 7 ("Housekeeping scan") at 10:21:32, and then the orchestrator manually posted Step 7 ("Review PRs 1806, 1807, 1809") at 10:25:40. PR review should be under Step 6 or 9, not 7. The post-step duplicate detection (PR #1245) should reject this — either it doesn't guard against automated-vs-manual collisions, or the orchestrator bypassed it. Single occurrence; not filing but monitoring.

3. **Cycle 368 pipeline FAIL temporal disruption.** C5.5 FAIL at 08:40:26 triggered a messy re-posting pattern: Steps 0.1 (08:41:08), 1 (08:41:14), and a "corrected" Step 0 (08:45:52) all appeared after the pipeline gate. This suggests the cycle-runner re-initializes after pipeline FAIL, posting startup steps retroactively.

4. **Pipeline improving.** Cycles 367-368 FAIL, cycle 369 PASS (3 warnings). The C5.5 FAILs appear to stem from doc-validation source-change-since warnings, which resolve when the orchestrator commits docs before running the gate.

## Cross-repo Observations

- Main active (7 PRs merged cycles 367-369), cycle 370 starting during this audit
- QC idle (sessions 503-506), justified
- QC filed [qc#505](https://github.com/EvaLok/schema-org-json-ld-qc/issues/505) not-applicable for #326

## Reflections

The tool-adoption-gap arc is the most instructive pattern class in audit history. Six recommendations, each accepted, each with infrastructure investment, and the behavioral gap only closed when the 6th recommendation proposed eliminating the behavioral requirement entirely (auto-inject). The progression:

1. #160: post-step tool built, not used same cycle → checklist enforcement
2. #164: post-step adoption not durable → NEVER-batch directive
3. #177: NEVER-batch insufficient → pipeline-check structural enforcement
4. #262: disposition reconciliation gap → --addresses-finding flag built
5. #324: flag built but never used → WARN + checklist reminder
6. #326: WARN insufficient → **auto-inject (eliminates behavioral requirement)**

Each step escalated the enforcement level. Only the final step — removing the human from the loop — achieved durable adoption. This is the definitive answer to "how do you get an LLM orchestrator to use a feature it built?": you don't. You make the feature use itself.

Zero open recommendations for the first time since cycle 151. The system is in a clean state.

## Tags

tool-adoption-gap-complete, auto-inject, acceptance-verification, step-id-collision, pipeline-improving
